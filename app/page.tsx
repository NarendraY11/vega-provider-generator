'use client';
import{useState}from'react';
export default function Home(){const[url,setUrl]=useState('');const[name,setName]=useState('');const[a,setA]=useState<any>(null);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[zip,setZip]=useState<any>(null);
async function analyze(){setLoading(true);setError('');setZip(null);try{const r=await fetch('/api/analyze',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url})});const d=await r.json();if(!r.ok)throw Error(d.error);setA(d);if(!name)setName(d.title||'Website Provider')}catch(e:any){setError(e.message)}finally{setLoading(false)}}
async function generate(){setLoading(true);setError('');try{const r=await fetch('/api/generate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({analysis:a,name})});const d=await r.json();if(!r.ok)throw Error(d.error);setZip(d)}catch(e:any){setError(e.message)}finally{setLoading(false)}}
function download(){const bin=atob(zip.base64);const bytes=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);const b=new Blob([bytes],{type:'application/zip'});const x=document.createElement('a');x.href=URL.createObjectURL(b);x.download=zip.filename;x.click()}
return <main className="wrap"><section className="hero"><span className="badge">VEGA • PROVIDER GENERATOR</span><h1>Turn a public website into a Vega provider.</h1><p>Paste a website URL. The generator analyzes public HTML and produces a Vega-compatible provider. For JavaScript-heavy sites, the generated provider can use Vega's built-in WebView renderer to read dynamically loaded content.</p></section><div className="card"><div className="row"><input className="input" placeholder="https://example.com" value={url} onChange={e=>setUrl(e.target.value)}/><button className="btn" onClick={analyze} disabled={loading||!url}>{loading?'Working…':'Analyze'}</button></div>{a&&<><div className="grid"><div className="panel"><h3>Website</h3><div>{a.title}</div><p className="muted">HTTP {a.status} • {a.contentType}</p></div><div className="panel"><h3>Detected media</h3><div className="success">{a.streams.length} direct streams • {a.videos.length} video tags • {a.iframes.length} embeds</div><p className="muted">0 direct streams does not necessarily mean the site has no video. Many modern sites create players after JavaScript runs; generated providers now have a WebView fallback for those cases.</p></div></div><div className="chips">{a.categories.slice(0,15).map((x:string)=><span className="chip" key={x}>{x}</span>)}</div><div style={{marginTop:18}} className="row"><input className="input" placeholder="Provider name" value={name} onChange={e=>setName(e.target.value)}/><button className="btn" onClick={generate} disabled={loading}>Generate ZIP</button></div></>}{error&&<div className="error">{error}</div>}{zip&&<div className="panel" style={{marginTop:16}}><h3 className="success">Provider generated successfully</h3><p className="muted">{zip.files.length} files created. Download the ZIP, put the provider folder into your Vega providers repository, update manifest.json, then run the official build/test commands.</p><button className="btn" style={{height:44}} onClick={download}>Download provider ZIP</button></div>}</div><div className="footer">Only use sources you are authorized to access. This tool does not bypass DRM, authentication, paywalls, or anti-bot protections. Generated providers should be reviewed and tested before use.</div></main>}
